
const sliderImages = new Swiper('.slider__images .swiper-container', { 
	
	direction: 'vertical',
    loop: true,
    autoplay: true,
    swipe: true,
    interval: 3000, 
	spaceBetween: 32, 
    
	grabCursor: true,
});
